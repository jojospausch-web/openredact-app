# NOTE: These dependencies are installed manually in Dockerfile
# due to torch version conflict from nerwhal (requires torch<1.6.0, unavailable on PyPI)
# and pydantic v2 incompatibility with FastAPI 0.65.2
# See Dockerfile for the five-stage installation process

# Standard packages (installed in Dockerfile Stage 2)
fastapi==0.65.2
uvicorn==0.11.7
python-multipart==0.0.5
click==7.1.1

# OpenRedact packages (installed in Dockerfile Stage 3 with --no-deps)
git+https://github.com/openredact/expose-text.git#egg=expose-text
git+https://github.com/openredact/nerwhal.git#egg=nerwhal
git+https://github.com/openredact/anonymizer.git#egg=or-anonymizer
